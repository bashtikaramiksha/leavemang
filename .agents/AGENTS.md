# Workspace Rules

## Vertical Slices & Implementation Plans
- Whenever the user provides details or requirements for a vertical slice (e.g. VS-01, VS-02, etc.), save the slice specification document in the `docs/` directory (e.g., `docs/slices/` or `docs/<slice-id>.md`).
- Additionally, create and save the corresponding implementation plan for that slice in `docs/`.
